## Lab Name & Date : Installing, updating, and removing software in Linux 11.2.25 ##

---
## Objective / Goal of the lab ##
- perform basic software maintenance on Linux machine
  - installing software not on the machine 
  - updating existing software to the newest version 
  - unistall software that's no longer needed 

---
## Tools used ##



___
##Steps I toke 

### Verifying application installation 
1. In Shell type `dpkg -s **program_name**` -> shows if installed or not
2. 

### Installing Firefox application 
1. 

---
## Screenshots ##


___
## Errors and the steps to correct ##


---
## Commands used
- `dpkg -s **program_name**` -> checks if application is installed 
- `sudo apt-get update` -> refreshes available software list so you get the newest version 
- `sudo apt-get install **package_name**` -> delete applications contents entirely 
- `sudo apt-get install -f` -> fixes broken or incomplete installations by installing needed dependencies 



---
## What I learned ##
